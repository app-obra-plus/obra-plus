import React from "react";
import { View } from "react-native";
import { colors } from "../theme/colors";

export default function HeaderBackground() {
  return <View style={{ flex: 1, backgroundColor: colors.support }} />
}
